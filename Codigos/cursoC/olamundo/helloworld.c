#include <stdio.h> // inclui uma biblioteca de interface de texto simple de interação com o usuario
#include <stdlib.h> // inclui uma biblioteca que possui comandos que se comunica com o computador como system(

int main(){
	printf("Hello, world!\n");
	// o comando printf imprime algo na tela, o \n é um compando para pular linha
	// return 0 (na interface[preta]) significa que o processo foi executado sem nenhum erro
	system("pause");
}
